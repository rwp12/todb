<?php
/*
    TODO:
    
    - Column aliases for special treatment columns in ShowGeneralTable():
      prefix the aliases with the use of the information, e.g. jobs_points
      for the special treatment of the points column when the
      view_jobs.php file calls the ShowGeneralTable function.

    - Put configuration information into arrays/objects for easier
      shifting around in the program (see table_functions.inc)
      
    - Put a switch into ShowGeneralTable which determines whether or
      not the table tags are output (<table ..></table).
      
    - Function per_person_count(): replace global variables with
      parameters.

*/

?>

